<!DOCTYPE html>
<html>
    <head>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/styles.css" type="text/css">
        <meta charset="utf-8">
        <title>Restaurants Guide</title>
    </head>
    <body>
        <div class="container">

            <div class="well navigation">
                <div class="left-header">
                    <h1>Welcome to Portland's restaurant guide</h1>
                </div>
                <div class="right-header">
                </div>
            </div>

            <div class="well middle">
                {% if cuisines is not empty %}
                    <h2>Select a cuisine</h2>
                    <ul>
                        {% for cuisine_type in cuisines %}
                            <li><a href="/cuisine_type/{{cuisine_type.getId}}">{{ cuisine_type.getCuisineName }}</a></li>
                        {% endfor %}
                    </ul>
                {%endif%}
            </div>

            <div class="well bottom">

                <h3>Input a cuisine!</h3>
                <form action="/" method="post">
                    <input type="text" name="new_cuisine">
                    <button type="submit" name="button">Add</button>
                </form>
            </div>
        </div>
    </body>
</html>
